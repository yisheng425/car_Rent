server:
    port: 8081

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/car_rent?useUnicode=true
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver

mybatis:
    mapper-locations: classpath:mapper/*.xml    #扫描所有mybatis的xml文件，里面写sql的